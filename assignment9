// 1. Difference between let, const, and var

function variableExample() {

    var a = "I am var";
    let b = "I am let";
    const c = "I am const";

    console.log(a);
    console.log(b);
    console.log(c);
}

variableExample();


// 2. Array of fruits and return second fruit

let fruits = ["Apple", "Banana", "Mango", "Orange", "Grapes"];

function getSecondFruit() {
    return fruits[1];
}

console.log(getSecondFruit());


// 3. Add element using push() and remove using pop()

function modifyArray(arr) {

    arr.push("New Element");
    arr.pop();

    return arr;
}

console.log(modifyArray([1, 2, 3]));


// 4. Square numbers using map()

let numbers = [1, 2, 3, 4, 5];

function squareNumbers(arr) {

    return arr.map(function(num) {
        return num * num;
    });
}

console.log(squareNumbers(numbers));


// 5. Filter odd numbers using filter()

function getOddNumbers(arr) {

    return arr.filter(function(num) {
        return num % 2 !== 0;
    });
}

console.log(getOddNumbers([1, 2, 3, 4, 5, 6]));


// 6. Object person and greeting message

let person = {
    name: "Shravani",
    age: 18,
    occupation: "Student"
};

function greetPerson(obj) {

    console.log(
        "Hello, my name is " + obj.name +
        ". I am " + obj.age +
        " years old and I am a " + obj.occupation + "."
    );
}

greetPerson(person);


// 7. Area of rectangle using object properties

function rectangleArea(rectangle) {

    return rectangle.width * rectangle.height;
}

console.log(rectangleArea({ width: 10, height: 5 }));


// 8. Return object keys using Object.keys()

function getObjectKeys(obj) {

    return Object.keys(obj);
}

console.log(getObjectKeys({
    name: "Rahul",
    age: 20,
    city: "Mumbai"
}));


// 9. Merge two objects using Object.assign()

function mergeObjects(obj1, obj2) {

    return Object.assign({}, obj1, obj2);
}

console.log(
    mergeObjects(
        { name: "Shravani" },
        { age: 18 }
    )
);


// 10. Sum of array using reduce()

let nums = [10, 20, 30, 40];

function calculateSum(arr) {

    return arr.reduce(function(total, num) {
        return total + num;
    }, 0);
}

console.log(calculateSum(nums));
